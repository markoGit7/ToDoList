############# Server ##############
npm install express (for server)
npm install --save-dev nodemon (for auto refresh on change)

############# DB ###################
npm install mysql2 (for database)

############# .env #################
npm install dotenv (for environment?)

############## files structure ########
your-project/
├── client/             # Frontend (React + Vite)
│   ├── src/
│   ├── public/
│   ├── .env            # Frontend .env (VITE_ vars only)
│   └── vite.config.js
│
├── server/             # Backend (Express + Node.js + MySQL)
│   ├── db.js           # MySQL database connection
│   ├── routes/         # API route handlers (optional, good for scaling)
│   ├── server.js       # Main Express app
│   └── .env            # Backend secrets: DB creds, JWT keys, etc.
│
├── package.json        # Might be at root, or separate in client/server
└── README.md

################ server.js ################
require('dotenv').config();
const express = require('express');
const mongoose = require('mongoose');

const app = express();
const PORT = process.env.PORT || 3000;

mongoose.connect(process.env.MONGO_URI)
  .then(() => console.log('MongoDB connected'))
  .catch(err => console.error('MongoDB error:', err));

app.use(express.json());

app.get('/', (req, res) => res.send('Server is live!'));

app.listen(PORT, () => {
  console.log(`Server running at http://localhost:${PORT}`);
});

##################### .env file ###########################
PORT=3000
MONGO_URI=mongodb://localhost:27017/mydb